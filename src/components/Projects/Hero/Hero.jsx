function Hero ({title}) {

    return (
        <section className="project-hero">
            <div className="std-wrapper">

                <div>{title}</div>

            </div>            
        </section>
    )
}

export default Hero;